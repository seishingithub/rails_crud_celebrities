<%= form_for @celebrity do |f| %>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :job %>
    <%= f.text_field :job %>
  </p>
  <%= f.submit 'Add celebrity' %>
<% end %>

<% @celebrities.each do |celebrity| %>
<ul>
  <li>
    <%= link_to "#{celebrity.name}", celebrity_path(celebrity) %>, <%= celebrity.job %>
  </li>
</ul>
<% end %>
